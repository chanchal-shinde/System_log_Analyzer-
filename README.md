Project Name: System Log Analyzer

->Description: Reads system log files, counts message types, detects repeated messages, generates a clean report.

->Technologies: Python (basic), file handling, string processing

->Features:
-Counts INFO, WARNING, ERROR messages
-Detects repeated messages
-Generates human-readable report
